* Issues

** テストが失敗する

README.txtに記載のある以下を実行する。

#+begin_src bash
  make compile
  make run_test
#+end_src

以下のようにテストが失敗する。

#+begin_src text
  gcc  -Wall -Isrc  -o testdiff src/test.c
  ./testdiff
  Testing md6parts.ComputeBounds...
  testdiff: src/test.c:203: testComputeBounds: Assertion `UB_in == 0' failed.
  make: *** [Makefile:10: run_test] 中止 (コアダンプしました)
#+end_src
